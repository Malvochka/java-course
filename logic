вывод программы:
static logic
logic (1) id= 0
logic (2) id= 1
ctor id= 100

Почему так?
1.Статический блок выполняется только при загрузке класса.
2. Нестатические блоки выполняются каждый раз при создании объекта. Они срабатывают до конструктора, но после инициализации полей значениями по умолчанию.
3.Поля инициализируются в порядке их объявления в коде. Конструктор вызывается последним, и он имеет доступ к окончательно инициализированным полям.
