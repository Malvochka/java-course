public class ByteExample {
    public static void main(String[] args) {
        int a = 120;
        byte b = (byte) (a + 10);  // (a + 10) = 130, приводим к byte (переполнение)
        byte c = (byte) (a + 10);  // Явное приведение (результат 130, преобразуем в byte)
        byte d = (byte) (a + 1);   // (a + 1) = 121
        System.out.println("b = " + b);  // b = -126 (переполнение byte)
        System.out.println("c = " + c);  // c = -126
        System.out.println("d = " + d);  // d = 121
    }
}
Объяснение:

В переменной b происходит переполнение, так как результат a + 10 = 130, и при преобразовании в byte он выходит за пределы диапазона от -128 до 127. Поэтому возникает переполнение и результат будет -126.
Переменная c тоже получает -126, так как явное приведение производит то же самое действие.
Переменная d — без переполнения, так как результат a + 1 = 121, что укладывается в диапазон byte.
